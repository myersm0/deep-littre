# Generated by the authoring harness. The versions in force when this store was
# written, for reading it later and for migrating it. Each record carries its own
# versions and hashes, and those are what gate whether a verdict applies.

structural_alternative_set_version = 1
closure_protocol_version = 1

[projections.block_text]
version = 2
description = "Direct content of one source block: descendant blocks and citations excluded, markup stripped, entity and character references decoded, whitespace runs collapsed."

[populations.structural_blocks]
version = 1
includes = ["indent", "variante"]
excludes = ["resume_indent", "resume_variante", "rubrique_indent", "rubrique_variante", "entete_nature"]

[populations.qualification_blocks]
version = 1
includes = ["indent", "variante"]
excludes = ["resume_indent", "resume_variante", "rubrique_indent", "rubrique_variante", "entete_nature"]

[passes.sublemma]
pass_version = 1
population = "structural_blocks"
population_version = 1
projection = "block_text"
projection_version = 2
exhaustive_extraction = true

[passes.voice_variant]
pass_version = 1
population = "structural_blocks"
population_version = 1
projection = "block_text"
projection_version = 2
exhaustive_extraction = true

[passes.qualification_scope]
pass_version = 1
population = "qualification_blocks"
population_version = 1
projection = "block_text"
projection_version = 2
exhaustive_extraction = false
